# Runtime dependencies for the Q-WiSE acoustic simulator web demo.
# Tracks the [project.dependencies] block in pyproject.toml — keep them in sync.
# Versions are pinned to the lower bound so HuggingFace Spaces (Python 3.10)
# resolves a known-good environment.

fastapi==0.115.2
uvicorn[standard]==0.32.0
websockets==13.1
pydantic==2.9.2
pydantic-settings==2.5.2
numpy==1.26.4
scipy==1.14.1
soundfile==0.12.1
onnxruntime==1.19.2
python-multipart==0.0.12
anyio==4.6.0
